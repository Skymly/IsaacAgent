# Spec: <子系统名>

> **版本**：v0.2.x（与 Git tag 对齐）
> **关联 Design Doc**：[docs/design/<Name>.md](../design/<Name>.md)
> **关联 ADR**：ADR-XXX（如有）

## API 面

### 公共接口

（接口定义、方法签名）

### 配置 / 契约

（环境变量、config.json 字段、工具 schema）

## 不变量

1. ...

## 兼容基线

- .NET 8
- Windows（App 层）

## 不在范围内

- ...
