<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:IsaacAgent.App.ViewModels"
        x:Class="IsaacAgent.App.Views.MainWindow"
        Title="IsaacAgent - Binding of Isaac Modding Assistant"
        Width="1200" Height="800"
        WindowState="Maximized"
        x:DataType="vm:MainViewModel">

    <Grid RowDefinitions="Auto,*,Auto">

        <Menu Grid.Row="0" Background="{DynamicResource SystemControlBackgroundAltMediumBrush}">
            <MenuItem Header="File">
                <MenuItem Header="New Project..." Command="{Binding NewProjectAsyncCommand}" />
                <MenuItem Header="Open Project..." Command="{Binding OpenProjectAsyncCommand}" />
                <Separator />
                <MenuItem Header="Settings..." Click="OnSettings" />
                <Separator />
                <MenuItem Header="Exit" Click="OnExit" />
            </MenuItem>
            <MenuItem Header="Chat">
                <MenuItem Header="Clear Chat" Command="{Binding ClearChatCommand}" />
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About IsaacAgent" Click="OnAbout" />
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1" ColumnDefinitions="250,*,300">

            <Border Grid.Column="0" BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}" BorderThickness="0,0,1,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Project Files"
                               FontWeight="Bold" Margin="10,8" />
                    <TextBlock DockPanel.Dock="Top" Text="{Binding Project.ProjectName}"
                               Opacity="0.6" Margin="10,0" />
                    <ListBox ItemsSource="{Binding Project.Files}"
                             Margin="0,8,0,0"
                             Background="Transparent">
                        <ListBox.ItemTemplate>
                            <DataTemplate x:DataType="vm:FileTreeItem">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <TextBlock Text="Lua" Foreground="#569CD6"
                                               IsVisible="{Binding IsLua}"
                                               FontSize="10" VerticalAlignment="Center" />
                                    <TextBlock Text="XML" Foreground="#CE9178"
                                               IsVisible="{Binding IsXml}"
                                               FontSize="10" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Padding="8">
                <DockPanel>
                    <ScrollViewer DockPanel.Dock="Top" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Chat.Messages}" Padding="8">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Spacing="8" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:ChatMessageViewModel">
                                    <Border CornerRadius="8" Padding="12,8"
                                            Background="{Binding BackgroundBrush}"
                                            HorizontalAlignment="{Binding HorizontalAlign}">
                                        <StackPanel Spacing="4">
                                            <TextBlock Text="{Binding RoleLabel}"
                                                       FontSize="11" Opacity="0.5" />
                                            <SelectableTextBlock Text="{Binding Content}"
                                                                 TextWrapping="Wrap"
                                                                 FontFamily="Cascadia Code,Consolas,monospace"
                                                                 FontSize="13" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Border DockPanel.Dock="Bottom" Padding="8,4" Margin="0,4,0,0">
                        <Grid ColumnDefinitions="*,Auto,Auto">
                            <TextBox Grid.Column="0"
                                     Text="{Binding Chat.InputText}"
                                     Watermark="Ask about Isaac modding..."
                                     AcceptsReturn="True"
                                     MinHeight="40"
                                     IsEnabled="{Binding !Chat.IsGenerating}" />
                            <Button Grid.Column="1"
                                    Content="Send"
                                    Command="{Binding Chat.SendCommand}"
                                    Margin="8,0,0,0"
                                    VerticalAlignment="Stretch"
                                    IsEnabled="{Binding !Chat.IsGenerating}" />
                            <Button Grid.Column="2"
                                    Content="Cancel"
                                    Command="{Binding Chat.CancelCommand}"
                                    Margin="8,0,0,0"
                                    VerticalAlignment="Stretch"
                                    IsVisible="{Binding Chat.IsGenerating}" />
                        </Grid>
                    </Border>
                </DockPanel>
            </Border>

            <Border Grid.Column="2" BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}" BorderThickness="1,0,0,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Quick Reference"
                               FontWeight="Bold" Margin="10,8" />
                    <ScrollViewer>
                        <StackPanel Spacing="4" Margin="10,0">
                            <TextBlock Text="Common Callbacks" FontWeight="SemiBold" Margin="0,8,0,4" />
                            <TextBlock Text="MC_POST_GAME_STARTED" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="MC_POST_UPDATE" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="MC_USE_ITEM" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="MC_POST_NEW_ROOM" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="MC_POST_PLAYER_UPDATE" FontFamily="Consolas" FontSize="11" Opacity="0.7" />

                            <TextBlock Text="Key Classes" FontWeight="SemiBold" Margin="0,12,0,4" />
                            <TextBlock Text="Isaac" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="Game()" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="EntityPlayer" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="EntityNPC" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="Room" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="Level" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="Vector" FontFamily="Consolas" FontSize="11" Opacity="0.7" />

                            <TextBlock Text="Mod Structure" FontWeight="SemiBold" Margin="0,12,0,4" />
                            <TextBlock Text="main.lua" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="metadata.xml" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="items.xml" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="entities2.xml" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="resources/" FontFamily="Consolas" FontSize="11" Opacity="0.7" />
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="{DynamicResource SystemControlBackgroundAltMediumBrush}" Padding="10,4">
            <TextBlock Text="{Binding StatusText}" FontSize="12" Opacity="0.7" />
        </Border>
    </Grid>
</Window>
